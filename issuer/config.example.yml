listen_port: 8080
bin_dir: .          # directory containing pre_personalize, personalize, read_personalized
nfc_device: ""      # leave empty to auto-detect; or e.g. "pn532_uart:/dev/ttyUSB0"
timeout: 30         # seconds to wait for a card before giving up
